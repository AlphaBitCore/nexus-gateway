{
  "name": "@nexus-gateway/db-migrate",
  "version": "0.1.0",
  "private": true,
  "description": "Database schema, migrations, and seeds for Nexus Gateway",
  "scripts": {
    "db:push": "prisma db push",
    "generate": "prisma generate",
    "seed": "prisma generate && tsx seed/seed.ts",
    "seed:prod": "SEED_DEMO=false npm run seed",
    "test": "tsx --test seed/__tests__/*.test.ts",
    "gen:rulepacks": "tsx seed/gen-rulepacks.ts",
    "check:rulepacks": "tsx seed/gen-rulepacks.ts -- --check",
    "gen:model-catalog": "node gen-model-catalog.mjs",
    "check:model-catalog": "node gen-model-catalog.mjs --check"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "dotenv": "^17.4.2",
    "pg": "^8.23.0",
    "prisma": "^7.9.1",
    "tsx": "^4.23.12",
    "yaml": "^2.9.0"
  }
}
